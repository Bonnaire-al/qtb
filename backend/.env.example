# Copier ce fichier en .env dans ce dossier : backend/.env
# Ne pas commiter .env (il contient des secrets).

# ========== ACCÈS ADMIN ==========
# Mot de passe demandé sur la page /admin (frontend).
# En local : mettez le mot de passe de votre choix.
# En déploiement : utilisez une variable d'environnement (ex. ADMIN_PASSWORD) sur votre hébergeur.
ADMIN_PASSWORD=1234.3421

# Port du serveur (optionnel ; défaut 5000)
# PORT=5000
